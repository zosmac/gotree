// Copyright © 2023 The Gomon Project.

package main

/*
Package gotree defines the gotree command that prints a tree listing of the processes on
the system. The tree organizes the processes by parent/child relationships. Each process in
the listing shows the pid and command name. A verbose listing includes each process' full
command path, arguments, the environment variables.
*/
